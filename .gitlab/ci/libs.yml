
include:
  - local: /.gitlab/ci/configs.yml

variables:
  PACKAGES: conv encoding engine flow fs http json ops regex text type

libs:release:
  stage: build
  extends:
    - .build-packages
  trigger:
    include: /.gitlab/ci/packages.yml
    forward:
      pipeline_variables: true