use core/arithmetic/scalar/f64::Sin as CoreSin

/**
Computes sine (in radians) of a stream of `f64`.

*/
sequence Sin()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreSin()

    Self.value -> CoreSin.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Cos as CoreCos

/**
Computes cosine (in radians) of a stream of `f64`.

*/
sequence Cos()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreCos()

    Self.value -> CoreCos.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Tan as CoreTan

/**
Computes tangent (in radians) of a stream of `f64`.

*/
sequence Tan()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreTan()

    Self.value -> CoreTan.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Asin as CoreAsin

/**
Computes arcsine (in radians) of a stream of `f64`.

Gives values in the range [0, pi] or not-a-number if outside range [-1, 1].

*/
sequence Asin()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreAsin()

    Self.value -> CoreAsin.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Acos as CoreAcos

/**
Computes arccosine (in radians) of a stream of `f64`.

Gives values in the range [0, pi] or not-a-number if outside range [-1, 1].

*/
sequence Acos()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreAcos()

    Self.value -> CoreAcos.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Atan as CoreAtan

/**
Computes arctangent (in radians) of a stream of `f64`.

Gives values in the range [-pi/2, pi/2].

*/
sequence Atan()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreAtan()

    Self.value -> CoreAtan.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Sinh as CoreSinh

/**
Computes hyberbolic sine of a stream of `f64`.

*/
sequence Sinh()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreSinh()

    Self.value -> CoreSinh.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Cosh as CoreCosh

/**
Computes hyberbolic cosine of a stream of `f64`.

*/
sequence Cosh()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreCosh()

    Self.value -> CoreCosh.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Tanh as CoreTanh

/**
Computes hyberbolic tangent of a stream of `f64`.

*/
sequence Tanh()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreTanh()

    Self.value -> CoreTanh.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Asinh as CoreAsinh

/**
Computes inverse hyperbolic sine of a stream of `f64`.

*/
sequence Asinh()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreAsinh()

    Self.value -> CoreAsinh.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Acosh as CoreAcosh

/**
Computes inverse hyperbolic cosine of a stream of `f64`.

*/
sequence Acosh()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreAcosh()

    Self.value -> CoreAcosh.value,value -> Self.value
}


use core/arithmetic/scalar/f64::Atanh as CoreAtanh

/**
Computes inverse hyperbolic tangent of a stream of `f64`.

*/
sequence Atanh()
  input  value: Stream<f64>
  output value: Stream<f64>
{
    CoreAtanh()

    Self.value -> CoreAtanh.value,value -> Self.value
}
